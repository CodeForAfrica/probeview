# Allow these dependencies to run their install/build scripts (pnpm blocks them
# by default). sharp = Next.js image optimizer; unrs-resolver = eslint-config-next.
allowBuilds:
  sharp: true
  unrs-resolver: true
