// Test stub for the `server-only` package, which throws when imported outside a
// React Server Component. Aliased in vitest.config.mts so server modules can be
// imported in unit tests. Intentionally empty.
export {};
